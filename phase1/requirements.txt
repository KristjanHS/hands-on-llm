# --- Core ML / GPU stack ---
# torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# these will not work as part of requirements.txt, since they are not available on PyPI
# instead, use the Makefile to install the correct version of torch
# or install manually:
# pip install torch==2.5.1+cu121 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121# torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# torchaudio optional: add if using it

# --- Jupyter + IPython ---
notebook
ipykernel
jupyterlab
ipython

# --- Pretty outputs ---
rich

# --- Required by torchvision, Jupyter ---
Pillow
matplotlib-inline
traitlets

# --- Optional scientific utilities ---
numpy
packaging
black
pytest

# --- Ollama ---
ollama
